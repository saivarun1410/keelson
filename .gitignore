node_modules/
dist/
*.log
.DS_Store
.claude/settings.local.json

# Note: docs/*.gif is deliberately NOT ignored — GitHub can only render the
# README hero image if the GIF is committed. Regenerate with:
#   cd demo && vhs hero.tape
